#include <bits/stdc++.h>
using namespace std;

void func(int num) { cout << endl << num; }

int main() {
    string name;
    getline(cin, name);
    cout << "hello, " + name << endl;
    int x;
    cin >> x;
    cout << "the value of x is: " << x;

    stack<int> s;
    func(18);
    return 0;
}
